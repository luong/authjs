{
  "name": "authjs",
  "version": "0.0.0",
  "private": true,
  "author": "luong",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm --stream -r run build",
    "dev": "pnpm --stream -r run dev",
    "start": "pnpm --stream -r run start"
  },
  "devDependencies": {
  },
  "pnpm": {
    "overrides": {
      "@auth/core": "0.18.3"
    }
  }
}
